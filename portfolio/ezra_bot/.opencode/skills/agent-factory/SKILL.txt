SKILL agent-factory | S01 | cluster orquestracao
DESC: Creates, instantiates and validates complete agents from design to blueprint with mandatory compliance.
OBJ: Create, instantiate and validate complete agents — from design to blueprint — ensuring every agent is born with persona, state, architecture, tests and compliance with the mandatory Agent Blueprint.
IN: New agent request (task, domain, autonomy level). Agent definition (persona, state, skills, template). Allocated resources (token budget, storage, permissions). Governance approval (High risk → mandatory HITL).
OUT: Instantiated agent with own directory. Generated Persona.md, state.json, startup.md. Validated optional skills and tests. Agent registered in orchestrator state.json. Agent Blueprint compliance report.
N8N INTEGRATION — MANDATORY RULE (approved Fabio 02/aug/2026): if an agent/workflow uses n8n, ENTER the n8n URL (`http://localhost:5678` local / public cloud URL). Always access n8n instance by URL — never try to bypass. n8n credentials in MCP: `N8N_EMAIL` (jae.engenharia@gmail.com) and `N8N_PASSWORD` — via `mcp_secrets_get`. Ezra MCP workflow server: `ezra-mcp` (id `8Q8c3ttyF4gPqijS`), endpoint `/webhook/mcp`. NEVER delete MCP credentials when touching integration (current security rule).
SKILL CREATION CRITERIA — GOLDEN RULE: BEFORE creating a new skill, ask Fabio. ALWAYS. Do not create without explicit authorization. Mandatory steps before asking: 1. Prove it does not exist: list all existing skills with similar subject and explain why each does NOT cover the case. 2. Prove it cannot be refined: show attempt to extend existing skill was impossible without mixing subjects. 3. Prove frequency: pattern repeated 3+ times in 7 days. A skill is approved only if: no existing skill covers the subject (proved). Cannot refine/extend existing skill without mixing subjects (proved). Minimum frequency 3+ times in 7 days (proved). SINGLE SUBJECT: one skill = one subject. Cannot mix responsibilities. Positive ROI (time saved/month > maintenance). Skill budget < 40; if >= 40, archive one first. Complete token budget, cost, security, tests and metadata.
DEPS: Governance.md (approval for High/Critical risk). SK-010 (Harness Architecture) for sandbox.
BUDGET: 2000-4000 tokens per complete creation (design → instantiation → validation).
COST: High. Creation involves multiple steps (design, instantiate, validate).
SEC: HIGH RISK. Agent creation with external execution requires mandatory HITL. Instantiated agent inherits parent orchestrator security restrictions. Blueprint validation is a mandatory gate — cannot be skipped. Every instantiation registered in governance-ledger.
TESTS: 1. Persona.md loads correctly? 2. State.json persists between executions? 3. 3 scenarios tested: success, failure, edge case? 4. Agent destruction cleans all artifacts? 5. Token budget does not exceed allocated? 6. Blueprint validation approved the agent?
