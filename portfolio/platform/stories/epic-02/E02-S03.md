---
story_id: E02-S03
epic: 02-ai-asset-ingestion-122-controls
status: ready-for-dev
priority: 3
estimate: L
linked_screens: [controls-questionnaire, score-dashboard]
linked_acs: [AC-02-D2]
risk_links: [R-2]
---

# Story: Questionário 122 controles L1–L4 + score

## Context
Auditor preenche os 122 controles (4 níveis) e vê o `agcp_score` (0–100) com gaps críticos.

## Sources of truth
`prd.md` tabela `Compliance_Assessments` + §1.1 North Star · `architecture.md` §2 · `tea-risk.md` R-2

## Acceptance criteria
- **AC-02-D2 (derivado):** Dado auditor em ativo ALTO RISCO, Quando preenche os controles L1–L4, Então o sistema calcula `agcp_score`, conta `gaps_critical` e mantém `status` coerente (`IN_PROGRESS`/`SUBMITTED`) sem permitir `CERTIFIED` com gap crítico.

## Restrictions
- **Out of scope:** relatório PDF → Epic 03.
- **Protected behaviors:** `controls_payload` (JSONB) versionado por avaliação.
- **Security:** autosave com `tenant_id` + `evaluated_by` auditados.

## Validation contract
- **AC → test:** AC-02-D2 → unit cálculo de score + integração transição de status com gap crítico.
- **Required checks:** lint, type-check, build.
- **States:** rascunho, submetido, bloqueado por gap.

## Notes for Shuri (Dev)
`apps/api/routers/assessments/`; cálculo em `libs/compliance/score`.

## Notes for Hawkeye (TEA)
Tests: 2 unit (score, contagem de gaps), 2 integração (transição, bloqueio).

## Done means
Score determinístico; gap crítico nunca gera `CERTIFIED`.