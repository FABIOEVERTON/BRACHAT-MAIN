---
story_id: E03-S02
epic: 03-custody-seal-report
status: ready-for-dev
priority: 2
estimate: M
linked_screens: [compile-report, manifest-view]
linked_acs: [AC-03-3]
risk_links: [R-4, R-10]
---

# Story: Hasher + validation_manifest + selo WORM idempotente

## Context
Hasher gera `validation_manifest.json`, calcula `master_hash_sha256` e grava PDF+manifesto no cofre WORM com idempotência por protocolo.

## Sources of truth
`prd.md` tabela `Forensic_Manifests` · `architecture.md` §3 passos 3–6 · `ADR-002` · `ADR-004` · `tea-risk.md` R-4/R-10

## Acceptance criteria
- **AC-03-3:** Dado envio do laudo assinado falha por rede, Quando o worker reexecuta, Então faz até 5 tentativas com chave de idempotência, sem duplicar registros periciais/financeiros, e só marca CONCLUÍDO após ETag do WORM confirmado.

## Restrictions
- **Out of scope:** verificação pública → E03-S03.
- **Protected behaviors:** `protocol_code` imutável e único; manifesto = fonte do hash mestre.
- **Security:** escrita WORM antes de qualquer entrega ao cliente.

## Validation contract
- **AC → test:** AC-03-3 → integração falha simulada no upload → 5 retries, 1 manifesto, sem duplicidade; integração ETag antes de CONCLUÍDO.
- **Required checks:** lint, type-check, build, idempotência.
- **States:** hashing, gravando, selado, falha→retry, DLQ.

## Notes for Shuri (Dev)
`apps/workers/hasher`; chave idempotente = `protocol_code`; DLQ após 5 falhas.

## Notes for Hawkeye (TEA)
Tests: 2 integração (retry idempotente, ETag-gate), 1 integridade (manifesto ↔ PDF).

## Done means
R-4 e R-10 mitigados; zero duplicidade pericial.