export default function LoginPage() {
  return (
    <main style={{ maxWidth: 420, margin: '6rem auto', textAlign: 'center' }}>
      <h1>EZRA — Login</h1>
      <p>(auth) shell — MFA/SSO entram na integração com auth.ts (FASE 1)</p>
    </main>
  );
}
