WORKLOG
"O que foi feito" - resposta padrao do bot para Fabio.

Ultima sessao (2026-08-07, anterior):
- Cerebro movido para OCI ezra_bot_1: opencode serve (127.0.0.1:3791) + bridge_telegram.service + parashat-bot.service + nblm-refresh.timer (systemd ativos).
- NotebookLM acessado de forma definitiva: notebooklm-py 0.8.0 + cookies persistentes (~/.notebooklm/profiles/default/storage_state.json); caderno TORAH_STUDIES e4274837-7838-4bbb-a490-8fde601e5c7a, 131 fontes.
- study.py integrado ao NotebookLM (_notebook_context, respostas com citacoes [1][2]); teste real Nitzavim = 3021 chars.
- Pipeline de sync Mac->Drive->GitHub->VM: sync-ezra.sh (rsync + git add .opencode/ + pull + restart ezra-serve na VM); bug do hook AGCP corrigido (|| true).
- Tabela de auditoria das 131 fontes: reference/audit_torah_sources.md.
- Telegram end-to-end comprovado: Fabio "OI" -> Ezra "Oi, Fabio. Estou pronto." via bridge -> serve OCI.
- Acquirer A1.Flex rodando no ezra_bot_2 (retry 5min, aguardando capacidade ARM).

Sessao atual (2026-08-07):
- Pending list reduzida a somente o que falta (done removidos: 3,4,30,32,44,45,46,48,50,53,54).
- Task 34: audit dos repos do Karpathy + leitura do spec Agent Plugins v1.0.0 -> reference/karpathy-audit.txt.
- Adotado plugin.json (spec Agent Plugins) na raiz de ~/.opencode (nome ezra, v1.0.0).
- Task 37 alterada: removido "Build OCI RAG agent", criado "criar agente do chalalange".
- Task 41 alterada: "verificar se conseguimos os 12 g (A1.Flex acquirer)".
- Regra REPORT adicionada ao EZRA_SYSTEM: bot consulta worklog/pending/state/ledger ao responder.
- Task 51 (aprovado): anti-reclaim deployado nas DUAS VMs (bot_1 e bot_2): /opt/anti-reclaim/anti-reclaim.py + systemd anti-reclaim.service (enable, active). Burst CPU 1 core + 200MB RAM, 20-40s a cada 60-240s, Nice=5 (nao rouba da producao).
- sync-ezra.sh agora inclui plugin.json (rsync da raiz).
- Praxe Portfolio adotada: ezra_bot e parashat_bot criados como projetos separados em brachat-main/portfolio (Google Drive). Commit d170ed0, push ok, VM atualizada. Task 52 done (versionamento via praxe Drive->git).
- .opencode virou GEMEA completa com o Mac: sync-ezra.sh agora rsync do diretorio inteiro (--delete); excluidos so node_modules, .DS_Store e credentials/ (segredos seguem so no Mac). state.json, governance-ledger, cache, data, logs, repos, config agora vao ao Drive. Git: .gitignore exclui segredos/estado (state.json, credentials, ledger, audit.log, logs, cache, data, repos). Commit 6f11488, sem segredos no git.
- AUTOMACAO do sync (aprovado): sync-ezra.sh ajustado para so dar pull/restart na VM quando ha commit. Agentes: launchd com.ezra.auto-sync (StartInterval 300s, RunAtLoad) + loop persistente nohup (PID ativo) com fingerprint md5 de ~/.opencode (exclui node_modules/credentials/.DS_Store). Loop confiavel ja funciona; launchd requer Full Disk Access p/ /bin/bash no System Settings (TCC bloqueia acesso ao Google Drive em agente de fundo). Log: /tmp/ezra-auto-sync.log. Lock: mkdir /tmp/ezra-auto-sync.lock.

Sessao atual (2026-08-09): BUG "bot nao executa" RESOLVIDO + WARMUP
- CAUSA RAIZ: o serve usa o opencode.json da RAIZ do repo (cwd=/home/ubuntu/brachat-main), nao .opencode/config/opencode.json (que era o que eu editava). Permission bash/edit/write estavam "ask" -> sem TTY o opencode auto-rejeita/hang e trava a fila (HTTP:000).
- FIX permissao: no opencode.json do repo, bash/edit/write -> allow (commit autorizado AGCP). Chave Composio hardcoded (ck_xt1sDN3kL-YoREV-P_VR, vazada) trocada por ${COMPOSIO_API_KEY}. Execucao verificada: /tmp/ezra-bot-prova.txt criado via bash tool do bot, HTTP:200 ~6.5s.
- WARMUP (nao determinístico): 1o request pos-restart as vezes >240s (provider free esfriando). Fixes:
  * ezra-serve.service: ExecStartPost roda /opt/ezra-warmup/ezra-warmup.sh (ping texto-só apos listening, curl ate 900s). Log: /tmp/ezra-warmup.log (ex. "done 11:02:18").
  * bridge_telegram.js: TIMEOUT 240s->600s, ack "(processando...)", retry com nova sessao quando a antiga fica invalida apos restart, dns.setDefaultResultOrder("ipv4first") (IPv6 do Telegram morto na rede da VM -> "poll fetch failed"; IPv4 ok).
  * opencode.json: composio enabled=false (header env literal quebra auth; bot nao usa).
- TESTE FINAL: restart serve -> warmup done -> request imediato HTTP:200 em 42s, arquivo criado. Servicos active: ezra-serve, bridge_telegram, anti-reclaim, parashat-bot.
- Commit: "bots: warmup ping no start, bridge timeout 600s + ack + retry sessao + ipv4first; composio disabled" (push ok). Arquivos em portfolio/ezra_bot/deploy/ (bridge_telegram.js, ezra-serve.service, ezra-warmup.sh).
- PENDENTE de seguranca: rotacionar/revogar chave Composio vazada no historico do repo.
